{
  "version_line": "3.14",
  "release_tag": "3.14-dev.1",
  "display_name": "PortaPy 3.14 Developer Preview 1",
  "stage": "developer-preview",
  "prerelease": true,
  "python_built_runtime": true,
  "native_targets": ["linux-x86_64", "windows-x86_64"],
  "source_execution_ready": false,
  "completed_surface": [
    "runtime handles",
    "environment handles aliasing runtime handles",
    "first-class native new and new_with_config helpers",
    "first-class native add and add_all binding helpers",
    "first-class native add_value_utf8 and add_callable_utf8 helpers",
    "first-class native execute evaluate and destroy helpers",
    "helper and low-level callback dispatch coexistence",
    "language-neutral C ABI usable from foreign-function interfaces",
    "host-owned module loading without a PortaPy import_module API",
    "host-registered import and from-import statements with aliases",
    "dotted import resolution through host object graphs",
    "ModuleNotFoundError and ImportError reporting for native imports",
    "None bool signed integer bit-exact binary64 UTF-8 string and bytes handles",
    "opaque host object and host callable handles with stable 64-bit IDs",
    "checked buffer copies retain release and runtime isolation",
    "public recursively-owned tuple dictionary and mutable list ABIs",
    "recursive Python tuple list and string-key mapping boxing and unboxing",
    "container globals snapshots and host callback round-trips",
    "retained native host global injection enumeration replacement and deletion",
    "native host attribute graph registration replacement lookup and dotted traversal",
    "synchronous qualified flattened and nested host callable dispatch",
    "borrowed callback arguments owned callback results and structured callback failures",
    "native import_binary and load_native Python facades",
    "automatic native add add_all add_modules expose and snapshot adaptation",
    "per-runtime structured errors and indexed native traceback frames",
    "NativeTracebackFrame objects and formatted native Python tracebacks",
    "PortaPy-owned lexer parser AST diagnostics and no-host-ast portable frontend",
    "portable exec eval and single compile modes",
    "portable module lowering in original source order",
    "portable scalar container slicing mutation deletion and unpacking expressions",
    "portable functions defaults decorators keyword calls positional and keyword variadics",
    "portable lexical closures captured cells isolated instances and persistent nonlocal state",
    "portable classes class variables inheritance constructors descriptors attributes and methods",
    "portable static methods class methods properties and data descriptors",
    "portable if while ordinary and tuple-unpacking for loop-else break and continue",
    "portable imports context managers raised exceptions typed handlers else and finally",
    "portable generators async functions await async generators and resumable execution",
    "portable comprehensions lambdas f-strings walrus expressions and structural matching",
    "canonical generated adapter from the public ABI into the portable frontend and full VM",
    "persistent independent full-VM namespace state per native runtime",
    "opaque retained handles for PortaPy-owned functions closures classes and instances",
    "full-VM host object proxies host callable proxies and host-added module resolution",
    "full-VM source-position metadata and traceback publication through the existing C ABI",
    "canonical build_native_typed command preparing and selecting the standalone VM by default",
    "pinned-compiler generated-source audit before native compilation",
    "hosted execution tests for the exact generated standalone environment",
    "canonical Linux and Windows matrix for advanced VM environments callbacks imports tracebacks floats and containers",
    "exact public export allowlists and Linux PIC no-TEXTREL transformations",
    "reproducible native builds pinned to a verified asmpython compiler commit"
  ],
  "release_blockers": [
    "obtain green hosted and pinned-compiler results for the canonical generated standalone adapter",
    "prove final Windows and Linux artifacts execute through the complete C matrix without loading or linking CPython",
    "verify final export tables no Linux TEXTREL headers examples artifact metadata and checksums",
    "promote version and release metadata from developer preview to final after verification"
  ]
}
